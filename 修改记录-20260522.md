# 修改记录 (2026-05-22 下午)

---

## 1. 模板1/4 行居中 + 单按钮居中

**文件:** DanmakuUIHelper.java

- 每行 HORIZONTAL LinearLayout 添加 `Gravity.CENTER` → 不满一行时按钮居中
- `resultContainer` 添加 `Gravity.CENTER_HORIZONTAL` → 单集按钮居中显示

---

## 2. 按钮标题首尾间距

**文件:** DanmakuUIHelper.java

- 标题首尾各加 **2个空格**（之前试了4个，感觉太多改2个）

---

## 3. 修复搜索缓存级联bug

**涉及文件:** EpisodeInfo.java, DanmakuScanner.java, DanmakuUIHelper.java

**问题:** 之前缓存键用了 `episodeNames[0]`，而 `episodeNames[0]` 可能是以前缓存的值（如"长安24计"），导致保存时 key 写成了缓存值而非原标题，旧的脏数据永远清不掉。

**修复:**
- `EpisodeInfo.java` → 新增 `originalTitle` 字段，存提取后的原标题
- `DanmakuScanner.java` → `setOriginalTitle(extractTitle2)`
- `DanmakuUIHelper.java` → 缓存保存和读取的 key 都改为 `originalTitle`，不再用 `episodeNames[0]`

---

## 4. 自动搜索回退（核心修复）

**文件:** LeoDanmakuService.java

**问题:** 文件名 `E60.No.Other.Love.2026` 中的 `E60` 被误识别为集数，导致搜索 URL 变成 `anime=xxx&episode=60`，API 返回空（电影没有第60集），自动推送失败。

**修复:** 先搜带集数的，搜不到自动降级：

```
自动搜索开始
  → 搜 anime=蜂巢的针&episode=60   ← 带集数，没结果
  → 搜 anime=蜂巢的针              ← 回退，不带集数
  → 拿到结果 → 按标题匹配 → 自动推送 ✅
```

回退模式下跳过集数过滤，只靠标题相似度匹配。
