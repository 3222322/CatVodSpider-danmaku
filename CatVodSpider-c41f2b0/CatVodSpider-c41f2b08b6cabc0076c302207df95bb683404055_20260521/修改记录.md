# 今日修改记录 (2026-05-21)

---

## WebServer.java — Web搜索页面UI

- **按钮布局**: 顺序改为 `[搜索] [输入框] [正序]`
- **间距**: `.search-box` 使用 `gap: 2ch`；搜索与正序之间 16dp 分隔
- **输入框**: `flex-grow: 1` → `flex: 2`（缩小1/3）
- **倒角**: 全部 `border-radius` 4px → 8px（容器 8px → 12px）

---

## DanmakuScanner.java — 按钮交互 & 菜单

- **长按"弹幕搜索"** → 直接显示远程搜索二维码（`showQRCodeDialog`），不再弹出菜单
- **菜单标题**: "Leo弹幕选项" → "弹幕设置"
- **`showLeoButtonMenu`**: `private` → `public static`，供 DanmakuUIHelper 跨文件调用

---

## DanmakuUIHelper.java — 搜索对话框 & 二维码对话框

### 搜索对话框布局（`showSearchDialog`）
- **⚙️ 齿轮图标**：
  - 改为纯 `TextView`，无底色，仅字符显示
  - 宽度 44dp → 29dp（缩小1/3），字号 22 → 15
  - 右侧 margin 8dp（与输入框间隔2字符）
- **输入词条**：
  - `weight=1` + 左右 margin 各 8dp（在⚙️和搜索按钮之间居中）
  - 底色：浅灰 `#E0E0E0`（亮色）/ `#44CCCCCC`（深色）
  - 文本居中对齐：`setGravity(Gravity.CENTER)`
  - 圆角背景：新增 12dp `GradientDrawable`
- **搜索按钮**：字号 14sp → 17sp（加大1号）
- **正序按钮**：字号 16sp → 14sp
- **按钮间距**：搜索与正序之间分隔符 4dp → 16dp（3字符间距）

### 全局倒角圆润
- 所有 `setCornerRadius` 统一增大：
  - 按钮/边框/页签/透明背景：12 → 18
  - 输入框焦点背景：8 → 12
  - 对话框主容器：新增 18dp 圆角 `GradientDrawable`

### 二维码对话框（`showQRCodeDialog`）
- URL 文字颜色设为白色
- URL 下方新增白色 "扫码搜索弹幕" 提示文字
- 二维码尺寸：400x400 → 533x533（放大1/3）

---

## 其他

- Android SDK 重建（重装 platform 36 + build-tools 35）
