# 重要修改记录 (2026-05-22)

---

## 1. 修复搜索缓存级联bug

**涉及文件:** EpisodeInfo.java, DanmakuScanner.java, DanmakuUIHelper.java

**问题:** 缓存键用了 `episodeNames[0]`，而 `episodeNames[0]` 可能是之前缓存的值（如"长安24计"），导致保存时 key 写成了缓存值而非原标题，旧的脏数据永远清不掉。

**修复:** 缓存键从 `episodeNames[0]` 改为 `originalTitle`（即 `extractTitle2` 提取后的原标题），始终对准原标题，不会再产生清不掉的脏数据。

---

## 2. 自动搜索回退机制

**文件:** LeoDanmakuService.java

**问题:** 文件名 `E60.No.Other.Love.2026` 中的 `E60` 被误识别为集数，搜索 URL 变成 `anime=xxx&episode=60`，API 返回空（电影没有第60集），自动推送失败。

**修复:** 先搜带 `&episode=60` 参数的，搜不到结果时自动去掉该参数重试，回退模式下跳过集数过滤，只靠标题相似度匹配。
